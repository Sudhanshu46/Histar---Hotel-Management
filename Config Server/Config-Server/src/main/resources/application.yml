server:
  port: 8085

spring:
  application:
    name: Config-Server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sauravkr1108/Hotel-Rating-App-ConfigServer
          clone-on-start: true